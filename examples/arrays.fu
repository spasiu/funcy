// Array operations example
@numbers [1, 2, 3, 4, 5];

@double n => n * 2;
@doubled map(numbers, double);
@output1 log(doubled);

@add_nums acc, n => acc + n;
@sum reduce(doubled, add_nums, 0);
@output2 log(sum);
